function App() {
  return (
    <main className="flex h-full flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-4xl font-semibold tracking-tight">Jacqline</h1>
      <p className="text-muted-foreground max-w-md text-base">
        Cross-platform desktop app to drive Claude sessions.
      </p>
      <p className="text-muted-foreground max-w-md text-sm">
        Scaffold OK — UI shell, projects CRUD, PTY terminal and file browser coming next.
      </p>
    </main>
  );
}

export default App;
