{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Jacqline window. Strict allowlist: only Tauri core defaults. All FS, PTY, bus, shell access goes through explicit Rust commands we register and scope ourselves.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
